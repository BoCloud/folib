<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.veadan.folib.mapper.ArtifactSyncSlaveRecordMapper">

    <resultMap id="artifactSyncSlaveRecordResultMap" type="com.veadan.folib.entity.ArtifactSyncSlaveRecord">
        <id property="id" column="id"/>
        <result property="sourcePath" column="source_path"/>
        <result property="targetPath" column="target_path"/>
        <result property="syncNo" column="sync_no"/>
        <result property="status" column="status"/>
        <result property="failedReason" column="failed_reason"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectListBySyncNoList" resultMap="artifactSyncSlaveRecordResultMap">
        select 
            id, source_path, target_path, sync_no, status, 
            failed_reason, create_by, create_time, update_by, update_time 
        from artifact_sync_slave_record
        where sync_no in
        <foreach collection="syncNoList" item="syncNo" open="(" separator="," close=")">
            #{syncNo}
        </foreach>
    
    </select>
    
    <delete id="batchDeleteBySyncNoList">
        delete from artifact_sync_slave_record
        where sync_no in
        <foreach collection="syncNoList" item="syncNo" open="(" separator="," close=")">
            #{syncNo}
        </foreach>
    </delete>

    <select id="selectBySyncNo" resultMap="artifactSyncSlaveRecordResultMap">
        select id,
               source_path,
               target_path,
               sync_no,
               status,
               failed_reason,
               create_by,
               create_time,
               update_by,
               update_time
        from artifact_sync_slave_record
        where sync_no = #{syncNo}
    </select>
</mapper>
