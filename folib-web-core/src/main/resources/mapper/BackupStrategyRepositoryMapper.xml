<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.veadan.folib.mapper.BackupStrategyRepositoryMapper">

    <insert id="batchInsertBackupStrategyRepository">
        INSERT INTO `backup_strategy_repository` ( `id`, `backup_strategy_id`, `storage_id`, `repository_id`, `create_by`, `create_time` )
        VALUES
        <foreach collection="backupStrategyRepositoryList" item="item" separator=",">
            (#{item.id}, #{item.backupStrategyId}, #{item.storageId}, #{item.repositoryId}, #{item.createBy}, #{item.createTime})
        </foreach>
    </insert>

</mapper>